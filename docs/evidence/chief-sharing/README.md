# Chief-sharing guidance

`mobile.png` is the actual Studio at a mobile viewport with synthetic member transport. The unavailable member view now directs a chief-managed dojo Coach to existing Dojo Chief sharing settings, with no separate external-agent permission.

Captured by `COACH_EVIDENCE_DIR=/home/kai/artifacts/chief-coach-browser npm run test:coach-browser`. The same suite exercises real local admin/Pi operator inference with a synthetic provider, navigation, cancellation, authorization fencing and member pagination. This image is not production or real-member data and does not establish backend authorization; the companion app regressions and Mongo/MCP seam cover that boundary.

The changed guidance assertion failed against the previous UI and passed after the correction. Full standalone suite: 235 passed; format, build and production-only package smoke passed.
