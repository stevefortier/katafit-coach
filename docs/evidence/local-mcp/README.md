# Local MCP Studio synthetic browser captures

Captured from the actual admin-served Studio with a disposable installation and synthetic loopback provider/server. The registration UI creates and lists a local Dojo registration, clears its bearer input, and explicitly says runtime is disabled pending backend authority. Future automatic side-effecting tool selection remains the intended UX without per-call approval, not a currently enabled capability. Desktop (1440px viewport) and mobile (390px viewport) section crops; these are not authenticated Kata.fit app screenshots or live model evidence. The registration was removed in the same browser smoke.
