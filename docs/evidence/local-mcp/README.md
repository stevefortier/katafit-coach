# Local MCP Studio synthetic browser captures

Captured from the actual admin-served Studio with a disposable installation and synthetic loopback provider/server. The registration UI creates and lists a local Dojo registration, clears its bearer input, and discloses automatic side-effecting tool selection without per-call approval. Desktop (1440px viewport) and mobile (390px viewport) section crops; these are not authenticated Kata.fit app screenshots or live model evidence. The registration was removed in the same browser smoke.
